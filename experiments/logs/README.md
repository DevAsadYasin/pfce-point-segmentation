This folder holds **`results.csv`** produced by `run_experiments.py` (and optional `--save-log` from `train.py`).

- For submission, include the latest `results.csv` after you run experiments, or ship the committed example bundled with the report.
